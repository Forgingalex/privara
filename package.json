{
  "name": "privara",
  "version": "0.1.0",
  "description": "Privacy-preserving reputation layer for social identity using Zama FHE",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@zama-ai/fhevm-hardhat-plugin": "^0.1.0",
    "hardhat": "^2.24.0"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  }
}
